var objF = new PraiseButton();
var objC = new Thumb();
describe("父类测试", function() {
  it("父类", function() {
    expect(objF.showGood()).toBe();
  });
});
describe("子类测试", function() {
  it("子类", function() {
    expect(objC.showGood()).toBe();
  });
});